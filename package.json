{
  "name": "@hcnc/common",
  "version": "1.0.2",
  "description": "",
  "main": "./build/index.js",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "del ./build/*",
    "pub": "git add . && git commit -m %npm_config_comment% && npm version patch && npm run build && npm publish"
  },
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^5.0.0",
    "typescript": "^4.9.5"
  }
}
